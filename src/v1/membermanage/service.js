const { token } = require('morgan');
const model = require('../../models/index');
const { v4: uuidv4 } = require('uuid');
const { default: axios } = require('axios');


//post data
exports.manageCredit = async (data) => {
   
}